#!/usr/bin/env bash
#
# Main installation script based on packages declared in `packages` file

source helpers

DOTFILES_ROOT=$(pwd -P)
packages_install "$DOTFILES_ROOT" "$DOTFILES_ROOT/packages"
success 'All done! :D'

exit 0
