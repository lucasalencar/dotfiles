{
  "hooks": {
    "Notification": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "tmux-notify-window"
          },
          {
            "type": "command",
            "command": "bash -c 'INPUT=$(cat); TYPE=$(echo \"$INPUT\" | jq -r .notification_type); [ \"$TYPE\" = \"auth_success\" ] && exit 0; MSG=$(echo \"$INPUT\" | jq -r \".message // empty\"); WIN=$(tmux display-message -t \"$TMUX_PANE\" -p \"#I\" 2>/dev/null); notify-macos \"Claude Code\" \"$MSG\" \"tmux select-window -t $WIN\" &'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "*.clj",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'lein lint 2>&1 | tail -20'"
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "bash /Users/lucas.alencar/.claude/statusline-command.sh"
  },
  "skipDangerousModePermissionPrompt": true
}
